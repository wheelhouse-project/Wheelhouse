# Copilot Instructions

At the start of each conversation, before responding to the user's first request, read and follow the instructions in `CLAUDE.md` (shared project rules for all AI agents) and `AGENTS.md` (agent workflow expectations).

This ensures every AI coding agent working in this repository receives identical project rules.
