"""Operator tools for the shared STT components.

Not part of the wheel: pyproject.toml packages only shared_stt and
shared_audio, so nothing here ships with a provider. These are run from a
checkout, against a machine the operator is standing at.
"""
