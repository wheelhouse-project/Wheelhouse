# WheelHouse Service

This is the core service of the WheelHouse application. It receives transcripts from the `google_stt_server`, interprets them as commands or dictation, and orchestrates all system actions. This includes managing application state, controlling the UI, and integrating with external hardware and services.
